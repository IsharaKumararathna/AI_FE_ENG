---
id: naming
title: Component Naming Conventions
tags: [naming, conventions, best-practice]
---

# Component Naming Conventions

Use PascalCase for all component names. Prefix with the category when ambiguous
(for example, `PrimaryButton`, `DataTable`). Avoid abbreviations unless they are
established domain terms. One component per file. The file name must match the
component name exactly.
